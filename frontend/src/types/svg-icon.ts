// Shared type for our inline native SVG icon components.
// Lucide removed brand icons (Github, LinkedIn etc.) so we embed them directly.
export interface SvgIconProps {
  size?: number;
  className?: string;
}
